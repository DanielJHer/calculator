
// calculator functions
add = (a,b) => a + b; 

subtract = (a,b) => a - b;

multiply = (a,b) => a * b;

divide = (a,b) => a/b;

// operator 
operate(x,y) {
if(add was selected) {
    add(x,y)
    return to display
}
}

// function that stores integer in display value
function stores(x) {
display.innerHTML.appendChild(x);
}

// UI for buttons
const display = document.getElementById('display');

const zero = document.getElementById('zero')
zero.addEventListener('click', );

letdisplayValue = 